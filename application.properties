# Configuração principal está em: src/main/resources/application.properties
# Valores padrão servem para desenvolvimento local (MySQL em 127.0.0.1).
# Para Docker: use o arquivo .env (veja .env.example) e compose.yaml.
